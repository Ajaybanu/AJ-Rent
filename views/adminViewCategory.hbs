 <div class="page-wrapper">
    

<main class="main">
        	<div class="page-header text-center" style="background-image: url('assets/images/page-header-bg.jpg')">
        		<div class="container">
        			<h1 class="page-title">admin<span>categories</span></h1>
        		</div><!-- End .container -->
        	</div><!-- End .page-header -->
            <nav aria-label="breadcrumb" class="breadcrumb-nav">
                <div class="container">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="/admin">Home</a></li>
                      
                        <li class="breadcrumb-item active" aria-current="page">Categories</li>
                    </ol>
                </div><!-- End .container -->
            </nav><!-- End .breadcrumb-nav -->

            <div class="page-content">
            	<div class="container">
					<table class="table table-wishlist table-mobile">
						<thead>
							<tr>
								<th>Category Image</td>
                                <th>Name</th>
								<th>Description</th>
                                <th>Count</th>
								<th>Edit</th>
                                <th>Delete</th>
							</tr>
						</thead>

						<tbody>
							{{#each categories}}
							<tr id="row{{this._doc._id}}">
                              
								<td class="product-col">
									<div class="product">
										<figure class="product-media">
											<a href="/admin/viewproductsC/{{this.id}}">
												<img src="/../{{this.image}}" alt="Product image">
											</a>
										</figure>

										
									</div><!-- End .product -->
								</td>
								   <td class="title-col">{{this.name}}</td>
                                <td class="Descrition-col">{{this.description}}</td>
								 <td class="Count-col">{{this.products.length}}</td>
                             
							    
                           
								
								<td class="Ed-col">
									{{!-- <form action="/addtocart/{{this.id}}" method="post">
									<button class="btn btn-block btn-outline-primary-2"><i class="icon-cart-plus"></i>Add to Cart</button>
				                   </form>				 --}}
								</td>

                                <td class="Edit-col"><a href="/admin/editcat/{{this.id}}" class="text-success"><i class="fas fa-edit fa-lg mx-1"></i></a></td>
                                <td class="Delete-col"> <a   href="/admin/deleteCat/{{this.id}}" class="text-danger"><i class="fas fa-trash fa-lg mx-1"   ></i></a></td>
							</tr>
							{{/each}}
							{{!-- --}}
						</tbody>
					</table><!-- End .table table-wishlist -->
	        
            	</div><!-- End .container -->
            </div><!-- End .page-content -->
			
        </main><!-- End .main -->
</div>

      <script>
    
           function deleteCat(productId){
            Swal.fire({
  title: 'Are you sure?',
  text: "You won't be able to revert this!",
  icon: 'warning',
  showCancelButton: true,
  confirmButtonColor: '#3085d6',
  cancelButtonColor: '#d33',
  confirmButtonText: 'Yes, delete it!'
}).then((result) => {
    if(res.data.msg_login){
        location.href="/admin"
    }
  else if (result.isConfirmed) {
             axios.get(`/admin/category/${productId}`,{
			}).then(response=>{
                if(response.data.msg_delete){
                    document.getElementById(`row${productId}`).style.display ='none'
                    document.getElementById('incorrects').innerHTML ="Category Deleted Succesfully "
                    document.getElementById('incorrects').style.display ='block';
                    setTimeout(() => {
                    document.getElementById('incorrects').style.display ='none' ;},3000);
                
                }
                if(response.data.msg_deleteerr){
                    document.getElementById('incorrect').innerHTML ="Unable To Delete Category "
                    document.getElementById('incorrect').style.display ='block';
                    setTimeout(() => {
                    document.getElementById('incorrect').style.display ='none' ;},3000);
                     }
                
                
            })
  }
  })
           }
           
        </script>
         <!-- jQuery -->
    <script src="/pluginsNA/jquery/jquery.min.js"></script>

    <!-- popper -->
    <script src="/pluginsNA/popper/js/popper.min.js"></script>

    <!-- bootstrap -->
    <script src="/pluginsNA/bootstrap/js/bootstrap.min.js"></script>

    <!-- slimscroll -->
    <script src="/pluginsNA/jquery-slimscroll/jquery.slimscroll.min.js"></script>

    <!-- uniform -->
    <script src="/pluginsNA/uniform/js/jquery.uniform.standalone.js"></script>

    <!-- switchery -->
    <script src="/pluginsNA/switchery/switchery.min.js"></script>

    <!-- theme core scripts -->
    <script src="/jsNA/main.js"></script>
        
		<script>
		   if({{msg_deleteerr}}){
                    document.getElementById('incorrect').innerHTML ="Unable To Delete Category "
                    document.getElementById('incorrect').style.display ='block';
                    setTimeout(() => {
                    document.getElementById('incorrect').style.display ='none' ;},3000);
                     }
            if({{msg_delete}}){
                    document.getElementById('incorrects').innerHTML ="Category Deleted Succesfully "
                    document.getElementById('incorrects').style.display ='block';
                    setTimeout(() => {
                    document.getElementById('incorrects').style.display ='none' ;},3000);
                     }         
    </script>